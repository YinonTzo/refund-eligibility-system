export default function SectionHead({ title }) {
  return (
    <div className="text-sub text-[10px] font-extrabold tracking-widest uppercase mb-4 pb-2.5 border-b border-border">
      {title}
    </div>
  );
}